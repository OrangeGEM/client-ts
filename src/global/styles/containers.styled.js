import styled from "styled-components";

/**
 * Container with direction in row
 */
export const RowContainer = styled.div`
    display: flex;
    flex-direction: row;
`

/**
* Container with direction in column
*/
export const ColumnContainer = styled.div`
    display: flex;
    flex-direction: column;
`
